fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlmodel>=0.0.16
sqlalchemy>=2.0.29
pydantic>=2.6.0
python-multipart>=0.0.9
PyJWT>=2.8.0
casdoor>=1.30.0
httpx>=0.27.0
python-dotenv>=1.0.1
psycopg2-binary>=2.9.9
pytest>=8.0.0
pytest-cov>=5.0.0
PyYAML>=6.0.1
cryptography>=42.0.0
alembic>=1.13.0

# [FIX] aiohttp روی پایتون <3.11 به async-timeout هم نیاز داره (وابستگی
# شرطی/marker-based). چون pip-compile اولیه رو با پایتون 3.12 اجرا کرده
# بودیم، این شرط false ارزیابی شده بود و pin/hash نشده بود. با uv
# pip compile --python-version 3.10 این مشکل رفع شد ولی برای اطمینان
# صریح هم اینجا نگه‌داری می‌شه.
async-timeout==5.0.1

websockets<17

# [FIX] pytest-cov نیاز به coverage[toml]>=7.10.6 داره (یه requirement با
# extras جدا از coverage ساده) که در حالت --require-hashes باید صریح pin
# بشه، وگرنه pip می‌گه "نسخه‌بندی نشده". با pin کردن دقیقاً همون نسخه‌ای
# که coverage ساده هم resolve شده (7.15.4)، هر دو با هم سازگار می‌مونن.
coverage[toml]==7.15.4
